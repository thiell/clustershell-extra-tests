# Runtime dependencies for clustershell-extra-tests.
#
# Pinned to ClusterShell upstream master: this repo is a canary that
# tracks master's Worker/Tree.py (the tests follow behavior changes
# like #594, the PEP 706 filter, and #663 as they land).

ClusterShell @ git+https://github.com/clustershell/clustershell.git@master
pytest>=7
coverage>=7
pytest-cov>=4
